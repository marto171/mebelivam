// 3D визуализации на проекти — изготвени по реални размери след оглед.
// Част от тях са реализирани, други са останали на етап проект.
const vizImages = Array.from(
  { length: 21 },
  (_, i) => `/mebelivam-img/визуализации/viz-${i + 1}.jpg`,
);

export default vizImages;
